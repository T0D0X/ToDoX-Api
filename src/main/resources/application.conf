db {
  host = ${DB_HOST}
  port = ${DB_PORT}
  name = ${DB_NAME}
  user = ${DB_USER}
  password = ${DB_PASSWORD}
}
test.port = 8080
test.db = "todo_test"
test.user = "test_user"
test.password = "test_password"
# Docker
postgres.image = "postgres:15"

# Данные(реализовать чтение отсюда в дальнейшем)