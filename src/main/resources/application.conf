db {
    host = ${DB_HOST}
    port = ${DB_PORT}
    name = ${DB_NAME}
    user = ${DB_USER}
    password = ${DB_PASSWORD}
    migration-enable = true
}
jwt {
    secret = ${JWT_SECRET}
    issuer = ${JWT_ISSUER}
    ttl = ${JWT_TTL}
}
auth {
    auth-token = ${AUTH_TOKEN}
}
