db {
    host = "localhost"
    port = "5432"
    name = "todo_test"
    user = "test_user"
    password = "test_password"
    migration-enable = true
}

jwt {
    secret = "secret"
    issuer = "issuer"
    ttl = 86400
}

auth {
    auth-token = "test"
}