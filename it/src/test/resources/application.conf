db {
    host = "localhost"
    port = "8080"
    name = "todo_test"
    user = "test_user"
    password = "test_password"

}

jwt {
    secret = "secret"
    issuer = "issuer"
    ttl = 86400
}

auth {
    auth-token = "test"
}